{
  "name": "tokyo-art-scene",
  "version": "0.6.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "8.1.3",
    "npm": "5.1.0"
  },
  "scripts": {
    "test": "eslint . && preact test",
    "start": "npm install && npm run -s serve",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch",
    "postinstall": "npm install && npm run serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^4.2.0",
    "eslint-config-synacor": "^1.0.1"
  },
  "dependencies": {
    "if-env": "^1.0.0",
    "preact-cli": "^1.4.0",
    "express": "^4.15.3",
    "preact": "^8.2.1",
    "preact-compat": "^3.16.0",
    "preact-router": "^2.5.5"
  }
}
